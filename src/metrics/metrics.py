import torch
import torch.nn.functional as F
import numpy as np
import random
import hydra
from torch.utils.data import DataLoader
from sklearn.metrics import accuracy_score, f1_score, precision_score, recall_score


def compute_xai_metrics(model, loader):
    # TODO: implementare la funzione che calcola le metriche XAI
    return
